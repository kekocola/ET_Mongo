项目初始化

这是一个空的git仓库，请帮我初始化项目，项目基础需求如下：
这是C# ET服务器 mongodb的配套管理工具，主要功能包括了玩家数据查询，玩家数据导出，玩家数据导入，玩家数据批量修改，mongo服务器性能数据查看，mongo连接管理等功能。
现在先完成以下需求：
1，初始化一个C# WPF windows 桌面应用程序，包括C# 工程，配套的编译bat脚本，单文件程序编译脚本，gitignore文件等。
2，初始化AGENTS.md文件，写入必要的内容，在每次功能开发完成后，都要维护AGENTS.md文件。
3，程序UI框架初始化，可以先不实现逻辑，搭建出基础的UI框架即可。核心UI交互逻辑包括以下内容：
3.1，MongoDB的连接管理，包括增加，删除，编辑；程序打开时有一个连接列表，没有连接默认需要新增连接。
3.2，连接完成后，打开工作区，工作区按照以上提到的主要功能做功能的切换操作，每个主题功能一个tab或者分页，这个设计取决于你。

